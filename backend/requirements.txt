# ---- Web 框架 ----
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# ---- LLM / 多智能体 ----
openai>=1.35                 # 讯飞星火 OpenAI 兼容接口直连
langgraph>=0.2.0             # 多智能体有向图编排(缺失时自动退化为内置 MiniGraph)
langchain-core>=0.3.0
langchain-openai>=0.2.0
websocket-client>=1.8        # 讯飞 TTS WebSocket
requests>=2.32
httpx>=0.27
pypdf>=4.2
rapidocr>=3.9,<4             # 本地 OCR 兜底,模型随包安装,无需外部密钥
onnxruntime>=1.20,<2         # RapidOCR CPU 推理引擎

# ---- 数据 ----
sqlalchemy>=2.0
psycopg2-binary>=2.9         # docker-compose 下连接 PostgreSQL;本地 SQLite 不需要
numpy>=1.26

# ---- RAG 可选重依赖(无 GPU/无网时自动降级,不装也能跑)----
# FlagEmbedding>=1.2         # BGE-M3 本地向量化
# pymilvus>=2.4              # Milvus 向量库

# ---- 测试 ----
pytest>=8.2
pytest-asyncio>=0.23
