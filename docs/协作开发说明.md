# SparkLearn 协作开发说明

这份文档给两个人或多个人协作时使用。核心原则是：不要所有人都直接改主目录，不要先写代码再猜接口。

## 先看这四份文档

```text
docs/api_contract.md
```

前后端接口契约。任何人改接口、SSE 事件、资源 payload、视频字段、评估字段前，都先看这份。

```text
docs/trace_debug_runbook.md
```

中间产物 trace 调试手册。效果差、资源没显示、视频不播放、画像没更新时，按这份文档查。

```text
docs/extra_sources_guide.md
```

多来源 RAG 资料接入指南。要把视频链接、网页、案例笔记、转写文本加入知识库时，按这份文档放文件。

```text
docs/代码结构说明.md
```

项目目录和模块说明。

```text
docs/测试说明书.md
```

后端和前端测试方式。

## 当前目录分工

主项目目录：

```text
C:\Users\26054\Desktop\sparklearn-multiagent
```

用途：稳定演示版本、统一合并版本、推 GitHub 的版本。

协作 worktree 目录：

```text
C:\Users\26054\Desktop\sparklearn-profile-path-eval
C:\Users\26054\Desktop\sparklearn-resource-rag-media
C:\Users\26054\Desktop\sparklearn-trace-debug
C:\Users\26054\Desktop\sparklearn-ui-polish
```

建议分工：

```text
sparklearn-profile-path-eval
  学生画像、学习路径、答题评估、学情闭环

sparklearn-resource-rag-media
  RAG 检索、图文教程、题目、脑图、Seedance/Manim 媒体

sparklearn-ui-polish
  前端页面、视觉、交互、AgentTrace 面板

sparklearn-trace-debug
  中间产物、日志、调试工具、运行可观察性
```

不要手动删除这些 worktree 文件夹。清理时要用 git 命令。

## 每个人开发前先做

进入自己的目录，例如：

```powershell
cd C:\Users\26054\Desktop\sparklearn-resource-rag-media
```

查看当前状态：

```powershell
git status
```

如果看到很多红色文件，先不要乱改，问负责合并的人。

## 接口改动规则

只要涉及下面任何一项，都先改 `docs/api_contract.md`：

```text
1. 新增 API
2. 修改请求 JSON
3. 修改返回 JSON
4. 修改 SSE 事件 type 或字段
5. 修改 ResourceItem payload
6. 修改 video.url / task_id / status
7. 修改 quiz question 字段
8. 修改 profile/path/eval 报告字段
```

接口改动顺序：

```text
先改 docs/api_contract.md
再改 backend
再改 frontend/lib/types.ts
最后改页面组件
```

不要让前端猜后端字段。

## 启动后端

第一次进入新的 worktree 后，需要自己建 Python 环境并安装依赖：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

后端地址：

```text
http://127.0.0.1:8000/docs
```

健康检查：

```text
http://127.0.0.1:8000/api/health
```

## 启动前端

第一次进入新的 worktree 后，需要安装依赖：

```powershell
cd frontend
npm install
npm run dev
```

前端地址：

```text
http://127.0.0.1:3000
```

前端会通过下面这个代理把 `/api/*` 转发到后端：

```text
frontend/app/api/[...path]/route.ts
```

## 中间产物在哪里

每次后端跑资源生成，都会自动写入：

```text
backend/runs/{session_id}/
```

重点看：

```text
request.json
events.jsonl
profile.output.json
planner.output.json
path.output.json
resources/
agent_end/
eval.output.json
final_state.json
summary.json
```

这些文件不要提交到 git，它们已经被 `.gitignore` 忽略。

## 调试顺序

如果页面没有反应，按这个顺序查：

```text
1. 后端健康检查是否 200
2. 前端页面是否 200
3. 浏览器 Network 里 /api 请求是否报错
4. SSE 事件里有没有 error
5. SSE 最后有没有 done
6. backend/runs/{session_id}/ 里中间产物是否完整
```

如果资源生成了但页面不显示，查：

```text
1. SSE 是否收到 resource
2. resource.kind 是否合法
3. resource.payload 是否符合 docs/api_contract.md
4. frontend/lib/types.ts 是否同步
5. ResourceCard 是否支持这个 kind
```

## 提交自己的修改

只提交自己负责的文件：

```powershell
git status
git add 你改过的文件
git commit -m "feat: 简短说明你做了什么"
```

不要随便用：

```powershell
git add .
```

除非你非常确认没有混入临时文件、数据库、运行日志、`.next`、`node_modules`。

## 合并回主目录

由一个人统一合并，建议在主项目目录执行：

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent
git merge feature/profile-path-eval
git merge feature/resource-rag-media
git merge feature/ui-polish
git merge feature/trace-debug
```

合并前先跑测试。

后端测试：

```powershell
cd backend
.\.venv\Scripts\python.exe tests\test_services.py
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_e2e_graph.py
```

前端至少确认：

```powershell
cd frontend
npm run build
```

## GitHub 同步

主目录推送到 GitHub 的分支是：

```text
sparklearn-main
```

推送命令：

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent
git push origin main:sparklearn-main
```

队友克隆：

```powershell
cd Desktop
git clone -b sparklearn-main https://github.com/vv510-ai/Cursor_Test.git sparklearn-multiagent
```
