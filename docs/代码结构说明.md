# SparkLearn 星火学伴 · 代码结构说明

## 1 仓库总览

```
sparklearn-multiagent/
├── backend/                          # FastAPI + LangGraph 多智能体后端
│   ├── app/
│   │   ├── main.py                   # 应用入口:建表、语料入库、静态挂载、注册路由
│   │   ├── config.py                 # 环境变量与运行模式(DEMO_MODE 自动判定)
│   │   ├── agents/                   # ★ 多智能体层
│   │   │   ├── state.py              #   全局 State(TypedDict + 并行 reducer)
│   │   │   ├── emitter.py            #   contextvar 事件总线(SSE 数据源)
│   │   │   ├── graph.py              #   LangGraph 编排图 + MiniGraph 兜底 + run_with_events
│   │   │   ├── orchestrator.py       #   编排器:X2 意图识别 + 条件路由
│   │   │   ├── profile_agent.py      #   画像:Lite 增量抽取 → 深合并 → profile 事件
│   │   │   ├── planner_agent.py      #   资源规划:生成什么/为什么(X2)
│   │   │   ├── path_agent.py         #   路径:调用 path_service 并广播 path 事件
│   │   │   ├── doc_agent.py          #   文档/代码/拓展阅读:RAG 引用 + 双层防幻觉
│   │   │   ├── mindmap_agent.py      #   思维导图:markmap Markdown
│   │   │   ├── quiz_agent.py         #   题库:结构化出题 + 本地校验 + 错因标签
│   │   │   ├── media_agent.py        #   媒体:分镜脚本 → Seedance / Manim+TTS 降级
│   │   │   ├── tutor_agent.py        #   答疑:OCR + 流式 + Mermaid 图解 + 溯源
│   │   │   └── eval_agent.py         #   评估:fan-in 质检落库 + evaluate_answers 闭环
│   │   ├── llm/                      # 模型与多模态网关
│   │   │   ├── spark_client.py       #   星火统一封装(互备/MockEngine/parse_json)
│   │   │   ├── signing.py            #   讯飞三元组 HMAC 签名
│   │   │   ├── spark_tts.py          #   语音合成(WebSocket)
│   │   │   ├── spark_image.py        #   文生图
│   │   │   ├── spark_ocr.py          #   拍照识题
│   │   │   └── seedance_client.py    #   火山 Seedance 异步视频任务
│   │   ├── rag/                      # 检索增强
│   │   │   ├── ingest.py             #   seed_corpus + extra_sources 解析/切片/入库
│   │   │   ├── embedding.py          #   BGE-M3 → 讯飞 → 哈希 三级降级
│   │   │   ├── vector_store.py       #   Milvus / 内存 双后端
│   │   │   ├── retriever.py          #   召回 + 可选重排 + 引用串
│   │   │   └── citation.py           #   编号上下文与 [^n] 统计
│   │   ├── safety/
│   │   │   ├── grounding.py          #   X2 答案-证据一致性裁决
│   │   │   └── guard.py              #   敏感词 + 讯飞审核挂接 + PII 脱敏
│   │   ├── services/                 # 领域服务
│   │   │   ├── bkt.py                #   贝叶斯知识追踪
│   │   │   ├── knowledge_graph.py    #   图谱载入/拓扑/解锁
│   │   │   ├── profile_service.py    #   八维画像持久化与合并
│   │   │   └── path_service.py       #   状态判定 + 打分排序 + 处方
│   │   ├── api/                      # 接口层(SSE 手写协议见 sse.py)
│   │   │   ├── chat.py resources.py path.py tutor.py evaluate.py meta.py
│   │   ├── models/                   # SQLAlchemy:db.py + entities.py(6 张表)
│   │   ├── schemas/core.py           # Pydantic 请求模型
│   │   └── data/                     # 知识图谱 / 种子讲义 / extra_sources / 题库
│   ├── tests/                        # 离线可跑:_stubs.py + 单元 + 端到端
│   ├── requirements.txt  Dockerfile
├── frontend/                         # Next.js 14(App Router,TypeScript)
│   ├── app/
│   │   ├── layout.tsx globals.css    # 深空蓝黑主题与导航壳
│   │   ├── page.tsx                  # 学习对话(左聊天 + 右遥测/画像 Tabs)
│   │   ├── resources/page.tsx        # 资源工坊(生成台 + 卡片墙)
│   │   ├── path/page.tsx             # 学习路径(DAG + 处方 + 重排)
│   │   ├── eval/page.tsx             # 学情评估(全景 + 事件流)
│   │   └── api/[...path]/route.ts    # BFF 流式代理(SSE 透传)
│   ├── components/
│   │   ├── agent/AgentTrace.tsx      # ★ 签名件:编排遥测面板 + 事件归约器
│   │   ├── chat/Chat.tsx Markdown.tsx
│   │   ├── resource/ResourceCard.tsx QuizPlayer.tsx Markmap.tsx VideoBlock.tsx
│   │   ├── path/PathDag.tsx          # 分层 SVG 知识图谱
│   │   └── profile/ProfileRadar.tsx  # 零依赖 SVG 雷达
│   ├── lib/ api.ts sse(postSSE) cdn.ts types.ts
│   ├── package.json  Dockerfile
├── docs/                             # 使用、测试、接口、协作、资料接入等文档
├── docker-compose.yml  .env.example  README.md
```

## 2 一次"生成请求"的完整数据流

前端资源工坊 POST `/api/resources/generate`(经 BFF 透传)→ `api/resources.py` 组装初始 State 并交给 `graph.run_with_events()` → 该函数创建请求级队列、把发射器写入 contextvar、后台执行编排图,同时把队列中的事件逐条 `data: {...}\n\n` 回写 → 图内:profile_agent 更新画像并 emit profile → orchestrator 路由 generate → planner 产出资源计划 → path_agent 重算路径并 emit path → LangGraph 把 doc/mindmap/quiz/media 四节点并发执行,各自 retrieve→生成→校验→emit resource,State 写入经 reducer 合并 → eval_agent 汇总 safety_flags、把资源批量 merge 进 resources 表、emit summary → run_with_events 收到完成哨兵,补发 done → 前端 Chat/工坊把 resource 事件落成卡片,AgentTrace 把 agent_* 事件折叠进遥测状态。

## 3 关键设计约定

LLM 访问只允许经 `llm_complete()/llm_stream()`,新增智能体不得自行创建客户端,这样互备、Mock 与计量在一处生效。事件一律通过 `emitter.emit()` 发出,字段以 `type` 区分,新增事件类型需同步更新前端 `lib/types.ts` 与 AgentTrace 的归约逻辑。State 中任何可能被并行节点写入的字段必须带 reducer 注解。JSON 列(payload/citations/safety/data)直接存取 Python dict/list,严禁手工 dumps。资源 `kind` 与生成节点的映射集中在 `graph.py` 的 `_KIND2NODE`。

## 4 扩展指南

新增一个智能体(例如"口述复述检查"):在 `agents/` 写 `async def run(state)->dict`,内部用 emitter 发 agent_start/agent_end;在 `graph.py` 注册节点与边(并行资源类同时加进 `_GEN_NODES` 与 `_KIND2NODE`);如需新资源类型,前端在 `ResourceCard.tsx` 的 KIND_META 与渲染分支各加一项即可。

更换课程:替换 `data/knowledge_graph.json`(节点 id/name/difficulty 与先修边)、清空并放入新课程的 `seed_corpus` 讲义、按需更新 `seed_quiz.json`,前端 `resources/page.tsx` 与 `ProfileRadar.tsx` 的知识点名称映射同步调整;视频链接、网页、案例笔记等补充材料放入 `data/extra_sources`,格式见 `docs/extra_sources_guide.md`。

接入新模型供应商:在 `spark_client.py` 的 MODELS 映射增加角色,或以相同函数签名实现新的客户端文件并在配置中切换。

## 5 开源组件与许可证清单

| 组件 | 用途 | 许可证 |
| --- | --- | --- |
| FastAPI / Starlette / Uvicorn | 后端框架与 ASGI | MIT / BSD-3 |
| LangGraph / LangChain-Core | 多智能体编排 | MIT |
| SQLAlchemy | ORM | MIT |
| Pydantic | 数据校验 | MIT |
| OpenAI Python SDK | 星火 OpenAI 兼容客户端 | Apache-2.0 |
| NumPy | 向量计算 | BSD-3 |
| requests / httpx / websocket-client | HTTP 与 WebSocket | Apache-2.0 / BSD / Apache-2.0 |
| BGE-M3 / bge-reranker(可选) | 向量化与重排 | MIT |
| Milvus / pymilvus(可选) | 向量库 | Apache-2.0 |
| Next.js / React | 前端框架 | MIT |
| Tailwind CSS | 样式 | MIT |
| react-markdown / remark-gfm | Markdown 渲染 | MIT |
| Mermaid(CDN) | 流程图渲染 | MIT |
| markmap-lib / markmap-view(CDN) | 思维导图渲染 | MIT |
| D3(CDN,markmap 依赖) | 可视化基础库 | ISC |
| Manim Community(用户本地渲染) | 降级动画 | MIT |

模型与云服务:讯飞星火大模型、讯飞 TTS/文生图/OCR、火山方舟 Seedance 按各自服务条款使用,密钥不入库不入代码。教材种子语料为团队自编开源讲义,以 CC BY-SA 4.0 发布于 `seed_corpus`,front matter 标注来源与许可。

## 6 AI 辅助开发声明

按赛题要求声明:开发过程中使用了 AI 辅助编码工具(讯飞 iFlyCode 等)进行代码补全与样板生成,所有生成代码均经人工审查、重构与测试;架构设计、算法选型与关键实现由团队完成并对其负责。
