# SparkLearn 星火学伴 · 测试说明书

## 1 测试概述

测试目标是验证五大功能(画像、资源生成、路径规划、智能答疑、学习评估)的正确性与闭环性,验证多智能体编排的并行与容错行为,验证防幻觉机制有效,并确认系统在无密钥离线环境与有密钥联网环境下均可稳定运行。测试方法包括单元测试、端到端冒烟测试、接口测试与人工演示走查。

## 2 测试环境

| 项 | 离线环境(演示模式) | 联网环境(真实模型) |
| --- | --- | --- |
| 操作系统 | Ubuntu 24.04 | Ubuntu 22.04 / Windows 11 |
| Python | 3.12 | 3.10+ |
| Node.js | — (后端测试不需要) | 20 LTS |
| 大模型 | MockEngine 本地模拟 | 讯飞星火 4.0Ultra / X2 / Lite |
| 向量库 | 内存存储 + 哈希 Embedding | Milvus + BGE-M3(可选) |
| 数据库 | SQLite | PostgreSQL 16 |
| 启动方式 | `python tests/*.py` | `docker compose up -d --build` |

## 3 单元测试

测试文件 `backend/tests/test_services.py`,离线零依赖可跑。

| 编号 | 用例 | 验证点 | 结果 |
| --- | --- | --- | --- |
| UT-01 | BKT 单调性 | 连续答对掌握度严格单调上升且突破 0.8 进入"掌握";答错后回落;始终在 [0,1] | 通过 |
| UT-02 | 知识图谱拓扑 | 16 节点全量载入;拓扑序中任意先修排在后继之前;无先修节点可被解锁 | 通过 |
| UT-03 | 路径阈值规则 | 掌握度 0.95 节点判 done;先修 0.65 的后继判 ready;next_kp 必为 ready 且存在;边引用的节点都在图中 | 通过 |

实测输出摘录:`test_bkt_monotonic ... ok / test_kg_topology ... ok / test_plan_path_thresholds ... ok`。

## 3.1 接口契约测试

测试文件 `backend/tests/test_api_contract.py`,用于防止后端真实返回字段、前端 `frontend/lib/types.ts`、`docs/api_contract.md` 三者漂移。

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent\backend
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe tests\test_api_contract.py
```

关键断言：

```text
SSE 事件 type 必须在契约白名单内
agent id 必须合法
resource 必须包含 id/kind/kp/title/payload/citations
doc/code/reading 必须有 payload.markdown
mindmap 必须有 payload.markmap
quiz 必须有 payload.questions
video 必须有 payload.script/video/manim_code
所有 SSE 最后必须是 done
```

## 4 端到端冒烟测试

测试文件 `backend/tests/test_e2e_graph.py`,在演示模式下走完整编排图(MiniGraph 等价并行)。

| 编号 | 链路 | 验证点 | 结果 |
| --- | --- | --- | --- |
| E2E-01 | 资源生成 | 事件序列含 profile→orchestrator→planner→path→doc/mindmap/quiz/media→eval 全部 agent_start;最后一个事件为 done;产出 doc/mindmap/quiz/video 四类资源;quiz 题组非空;doc 携带引用;4 份资源全部落库 | 通过(30 事件 / 4 资源 / 4 行) |
| E2E-02 | 智能答疑 | tutor 分支不进入生成流水线;流式 token ≥5 段;citations 事件携带教材引用 | 通过(9 段 token) |
| E2E-03 | 评估闭环 | 提交 5 题(3 对 2 错)后:accuracy=0.6;binary_tree 掌握度 0.300→0.838;错因标签写入画像 error_prone;返回重排路径与学习建议 | 通过 |

## 5 功能测试用例(演示走查)

### 5.1 学生画像

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| F1-1 | 首页输入"我是大二学生,两周后期末考,偏好看图理解,总错在递归边界" | 右侧 PROFILE 页签:目标=期末备考、认知风格=视觉型、易错含"递归边界",版本号 +1 |
| F1-2 | 完成一组答错的题 | error_prone 出现对应错因标签;雷达图薄弱点掌握度下降 |
| F1-3 | GET /api/profile/demo_user | 返回完整八维 JSON,knowledge_mastery 覆盖 16 个知识点 |

### 5.2 资源生成

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| F2-1 | 资源工坊选「二叉树」+ 四类资源,点开始生成 | AgentTrace 中 DOC/MAP/QUZ/MED 四节点同时进入呼吸态(并行可见);完成后四张卡片落地 |
| F2-2 | 展开图文教程卡片 | Markdown 含章节、Mermaid 图、[^n] 脚注;页脚 CITATIONS 列出《数据结构与算法》章节来源 |
| F2-3 | 展开思维导图卡片 | markmap 交互脑图可缩放展开;CDN 不可达时降级为大纲文本 |
| F2-4 | 未配置火山密钥时展开视频卡片 | 呈现分镜脚本 + Manim 降级代码(可复制)+ 旁白文本;配置密钥后呈现任务轮询与最终视频 |
| F2-5 | 生成完成后刷新「我的资源库」 | 新资源出现在历史列表(数据库持久化) |

### 5.3 路径规划

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| F3-1 | 打开学习路径页 | DAG 分层渲染;done 薄荷绿 / ready 电光蓝 / locked 灰;橙色虚线框标注下一步推荐 |
| F3-2 | 点击任意节点 | 右侧显示掌握度进度条、状态与自然语言推荐理由(处方) |
| F3-3 | 点击"按最新画像重排" | next_kp 与推荐序列随当前掌握度变化 |

### 5.4 智能答疑

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| F4-1 | 提问"为什么 Dijkstra 不能处理负权边" | 逐 token 流式输出;先思路后结论;末尾附 Mermaid 图解与教材引用 |
| F4-2 | 上传题目照片(联网环境) | OCR 还原题面出现在进度提示;回答针对该题 |
| F4-3 | 提问超纲内容 | 回答标注溯源提示而非编造教材出处 |

### 5.5 学习评估

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| F5-1 | 在题组卡片作答并提交 | 即时逐题判分,错题显示参考答案、详解与错因标签 |
| F5-2 | 提交后查看报告区 | 显示正确率与该知识点新掌握度;提示路径已重排 |
| F5-3 | 打开学情评估页 | 16 知识点掌握全景条形、累计轮次、平均正确率、近期事件流齐全 |

## 6 接口测试

| 接口 | 方法 | 关键断言 |
| --- | --- | --- |
| /api/health | GET | status=ok;demo_mode 与密钥配置一致 |
| /api/chat | POST(SSE) | 帧格式 `data: {json}\n\n`;终帧 type=done |
| /api/resources/generate | POST(SSE) | resource 事件数 ≥ 所选类型数 |
| /api/resources?user_id= | GET | items 倒序;payload 为 JSON 对象而非字符串 |
| /api/path /api/path/replan | GET/POST | nodes16 + edges19;replan 后 updated_at 变化 |
| /api/eval/submit | POST | 返回 accuracy/per_kp/suggestions/path |
| /api/eval/report | GET | radar 长度 16;attempts.accuracy ∈ [0,1] 或 null |
| /api/kg | GET | nodes 以 id 为键;edges 为 from/to 数组 |

## 6.1 教材式联调脚本

按课程里的方法,先验证真实接口字段和 SSE 事件,再继续改功能。后端目录执行:

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent\backend
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe scripts\debug_api_flow.py
```

这个模式不需要启动 HTTP 服务,会直接跑后端生成链路,并检查:

```text
1. RAG 语料是否已入库
2. profile → orchestrator → planner → path → doc/mindmap/quiz/media → eval 是否完整
3. SSE 最后是否有 done
4. doc/mindmap/quiz/video 四类资源是否产出
5. 文档资源是否带 citations
```

如果要调真实后端接口,先启动后端,再执行:

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent\backend
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe scripts\debug_api_flow.py --base-url http://127.0.0.1:8000
```

如果要调前端代理层,先同时启动前端和后端,再执行:

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent\backend
$env:PYTHONIOENCODING="utf-8"; .\.venv\Scripts\python.exe scripts\debug_api_flow.py --base-url http://127.0.0.1:3000
```

## 7 性能与并发观测

演示模式下(模拟打字机间隔 12ms),生成链路端到端约 4–6 秒完成 30 个事件;答疑首 token 小于 200ms。联网环境的预算指标为:答疑首 token ≤2s(星火流式),四资源并行生成总时长 ≈ 单个最慢资源(文档,约 20–40s)而非四者之和,视频走异步任务不计入阻塞时长。SSE 连接在 Nginx/Node 代理下需关闭缓冲(已在响应头设置 `X-Accel-Buffering: no`),实测无粘包丢帧。

## 8 防幻觉专项测试

构造三类输入验证双层校验:其一,正常知识点生成,grounding 判定 grounded=true,卡片无复核标记;其二,在演示语料外追问细节,生成文末出现"溯源提示"块且资源标记"溯源复核";其三,在输入中夹带敏感词,safety 事件触发、safety_flags 落库。引用脚注 [^n] 与页脚 CITATIONS 编号一一对应,抽查 10 处引用均能在 `seed_corpus` 对应章节找到原文依据。

## 9 兼容性

前端在 Chrome 120+/Edge 120+ 验证通过;Firefox 下 SSE 与 SVG 渲染正常;1280px 以上桌面为主要适配目标,窄屏自动隐藏右侧遥测栏。后端在 Python 3.10–3.12 验证;SQLite 与 PostgreSQL 双库通过同一套 ORM 跑通。

## 10 缺陷记录与回归

| 编号 | 现象 | 根因 | 修复 | 回归 |
| --- | --- | --- | --- | --- |
| BUG-01 | 编排在 planner 后中断 | f-string 内字典下标漏引号(`plan[resources]`) | 加引号并全仓正则扫描同类写法 | E2E-01 通过 |
| BUG-02 | 题组只剩 1 道兜底题 | parse_json 恒先匹配 `{`,顶层 JSON 数组被截成首个对象 | 按 `{`/`[` 先出现者决定截取顺序 | E2E-01/03 通过 |
| BUG-03 | media 节点不执行 | kinds 中的 video 未映射到 media 节点 | 建立 kind→node 映射表(code/reading→doc,video/audio→media) | E2E-01 通过 |
| BUG-04 | 答题落库报字段错误 | QuizAttempt 实体为整卷 score+detail,接口按逐题字段写入 | 接口改为整卷一条记录,报告端同步改聚合口径 | E2E-03 通过 |
| BUG-05 | 资源详情 payload 双重编码 | JSON 列已自动序列化,代码又手工 dumps | 直接存取 dict,并加历史数据兼容读取 | F2-5 通过 |

全部缺陷修复后,单元测试 3/3、端到端 3/3 全部通过,作为基线纳入回归集。

## 11 测试结论

系统五大功能在离线演示模式与设计的联网模式下均满足需求;多智能体并行编排、SSE 实时遥测、防幻溯源与评估闭环等关键特性经端到端验证有效;已知缺陷全部闭环。系统达到参赛演示与评审标准。
