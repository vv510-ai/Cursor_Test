# 多模态与容器验收清单

更新时间：2026-07-13。该清单记录真实执行结果，不以演示数据替代外部服务验收。

## 1. 当前结论

- [x] 多模态错误可观测：OCR/文生图失败保留 `code/message/sid`，签名 URL 与密钥不写日志。
- [x] 协议对齐：图片理解默认 `domain=general`；文生图统一 `768x768`；图片上传限制 4MB。
- [x] 故障隔离：多模态使用独立 4 线程池，槽满快速降级；OCR/TTS 有供应商级看门狗。
- [x] HMAC 时区修复：使用 Unix 时间生成 GMT 日期；上海时区真实 TTS 调用成功。
- [x] 后端离线回归：`37 passed`；多模态网关、签名与媒体降级定向回归：`17 passed`。
- [x] 前端生产构建：`npm run build` 通过。
- [x] 本地 BFF：`GET http://127.0.0.1:3000/api/health` 返回 200，并转发至 8010 后端。
- [x] 静态媒体 BFF：`/static/gen/*` 经 `/api/static/gen/*` 同源流式透传，MP4 Range 请求返回 `206 Partial Content`。
- [x] TTS 金样：真实生成成功，`cached=false`，输出文件非空。
- [x] 视频安全兜底：Seedance 未配置时，`binary_tree`/`bst` 使用预渲染 Manim MP4，同时保留脚本、Manim 源码与真实 TTS。
- [x] 真实资源链路：经 Next BFF 生成二叉树视频资源，SSE 为 `profile=1/path=1/resource=1/summary=1/done=1/agent_start=6/agent_end=6`，无 `error`。
- [x] 浏览器实测：本地 Manim MP4 与 TTS 均 `readyState=4`、`error=null`，控制台错误为 0。
- [ ] OCR 金样：当前 APPID 返回 `11200 AppIdNoAuthError: tokens.total`，需在讯飞控制台开通图片理解或领取对应额度。
- [ ] 文生图金样：当前 APPID 返回 `11201 AppIdNoEnoughLicenceError: tti`，需领取/购买图片生成点数。
- [ ] Seedance：未配置 `ARK_API_KEY/SEEDANCE_MODEL`，保持 Manim + TTS 安全降级。
- [ ] Docker 真机：当前 Windows 环境没有 `docker`/`docker compose` 命令，尚未实际启动容器。

## 2. 金样真实输出

执行命令：

```powershell
cd C:\Users\26054\Desktop\sparklearn-multiagent\backend
.\.venv\Scripts\python.exe tests\smoke_multimodal.py --golden
```

本次结果：

| 能力 | 样本 | 结果 | 服务端证据 |
| --- | --- | --- | --- |
| OCR | `lecture_binary_tree.png` | 失败并安全降级 | `code=11200`, `sid=cht000bff3c@dx19f59f684459a4b550`, `tokens.total` |
| OCR | `lecture_sorting.png` | 失败并安全降级 | `code=11200`, `sid=cht000cff0e@dx19f59f6978bb8f3550`, `tokens.total` |
| TTS | `narration.txt` | 通过 | `cached=false`，真实生成 MP3 |
| 文生图 | `binary-tree` | 失败并安全降级 | `code=11201`, `sid=cht000b9435@dx19f59f6a976b894550`, `tti` 授权不足 |
| 文生图 | `sorting` | 失败并安全降级 | `code=11201`, `sid=cht000b9436@dx19f59f6ae2ab894550`, `tti` 授权不足 |

金样模式返回非零退出码是正确行为：它证明外部能力尚未满足验收标准，而不是主链路崩溃。开通权限后重复执行同一命令，必须看到 `GOLDEN_PASS`。

## 3. Docker 安装后执行

在仓库根目录运行：

```powershell
docker compose up -d --build
docker compose ps
Invoke-WebRequest http://127.0.0.1:8000/api/health -UseBasicParsing
Invoke-WebRequest http://127.0.0.1:3000/api/health -UseBasicParsing
docker compose logs --tail=100 backend frontend
```

容器内运行金样（只读挂载测试目录，不把测试文件打进生产镜像）：

```powershell
docker compose run --rm --volume "${PWD}/backend/tests:/srv/tests:ro" backend python tests/smoke_multimodal.py --golden
```

关闭容器但保留数据库：

```powershell
docker compose down
```

不要在仍需保留数据时使用 `docker compose down -v`。

## 4. 页面全链路核对

- [ ] `http://127.0.0.1:3000/`：学习对话可流式输出；多模态失败时文本答疑仍可继续。
- [x] `http://127.0.0.1:3000/resources`：最新视频资源可展开；本地 MP4、真实 TTS 与 Manim 源码均正常显示。文生图无授权时只缺封面，不影响脚本与媒体兜底。
- [ ] AgentTrace：`profile -> planner -> path -> doc/mindmap/quiz/media -> eval` 正常折叠显示，无新增事件类型。
- [ ] `http://127.0.0.1:3000/eval`：做题后正确率、掌握度与错因可见。
- [ ] `http://127.0.0.1:3000/path`：路径顺序与解锁状态读取最新画像。
- [x] 本次 `/resources` 浏览器验收控制台无错误；后端多模态日志只记录 capability/trace/status/耗时/裁剪后的错误，不记录密钥或签名 URL。

## 5. 回归命令

```powershell
cd backend
$env:DEMO_MODE='true'
$env:EMBEDDING_BACKEND='hash'
.\.venv\Scripts\python.exe -m pytest -q

cd ..\frontend
npm run build
```

## 6. 故障演练结果

三套演练均在独立进程中覆盖环境配置，不改 `.env`，不写假成功：

| 场景 | 实测结果 | 主链路策略 |
| --- | --- | --- |
| `DEMO_MODE=true` 且无任何密钥 | OCR 返回既有演示题面；TTS/文生图/视频返回 `degraded=true`，耗时 0ms | 文本与本地演示链继续，外部媒体不阻塞 |
| 仅保留文本大模型密钥 | OCR/TTS/文生图/视频全部返回 `credentials not configured`，无供应商调用 | 资源生成继续使用脚本 + 本地 Manim，媒体字段按条件缺省 |
| 单独关闭 `MM_TTS_ENABLED=false` | TTS 立即返回 `capability disabled`、`degraded=true`，耗时 0ms | 只缺旁白，不影响视频脚本、Manim 与其他资源 |

此外，真实账号演练结果为：TTS 成功；OCR 返回 `11200` 权限不足；文生图返回 `11201` 额度不足；Seedance 未配置。四种结果均被网关归一化，不向资源主链路抛异常。
