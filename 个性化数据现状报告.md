# SparkLearn 个性化数据现状报告

> 调查日期：2026-06-25  
> 调查范围：当前仓库源码、`backend/sparklearn.db` 只读查询、当前 `127.0.0.1:8010` 只读 API、已有 `backend/runs/` 运行留痕。  
> 本轮操作边界：未修改源码、未修改数据库、未删除记录、未重建数据；唯一新增内容是本报告。

## 0. 结论摘要

当前项目的“答题 → 后端判分 → BKT → 画像落库 → 路径重排”代码接线已经存在，而且对**规范知识点 ID、可从数据库还原的题组、当前版本客观题**而言，数据流是同源的。

但是 `demo_user` 不能再被当作一个真实学生的可信画像，原因不是单个字段脏，而是三类数据混在了一起：

1. 人工浏览器操作；
2. 历史版本逻辑产生的数据；
3. 自动 smoke/联调故意制造的全对、全错记录。

五条旧问题的当前结论：

| 疑似问题 | 当前结论 |
| --- | --- |
| `complexity` 与 `complexity(复杂度分析)` 并存 | **仍存在，已定位根因** |
| 错因标签被拆成“概、念、混、淆” | **仍存在，已定位确定性代码根因** |
| `resource_pref` 全是 0 | **旧现象已变化，但当前更不合法：`mindmap=31`** |
| 每日学习时间为 4 分钟 | **仍存在；不是计时公式算错，而是没有时长证据时由 LLM 推断并无校验落库** |
| 前端固定使用 `demo_user` | **仍存在，首页/资源/路径/评估/答题全部共用** |

因此，当前个性化闭环属于“工程链路已通、学生数据可信度不足”。在清洗前，页面上显示的推荐路径和画像不能作为真实个性化效果证明。

## 1. 本轮实际读取内容

### 1.1 仓库规则与结构

- `AGENTS.md`
- `docs/代码结构说明.md`
- `agents/sparklearn-live-integration/SKILL.md`

### 1.2 后端画像、评估与路径源码

- `backend/app/services/profile_service.py`
- `backend/app/services/bkt.py`
- `backend/app/services/knowledge_graph.py`
- `backend/app/services/path_service.py`
- `backend/app/agents/profile_agent.py`
- `backend/app/agents/eval_agent.py`
- `backend/app/agents/quiz_agent.py`
- `backend/app/agents/mindmap_agent.py`
- `backend/app/agents/planner_agent.py`
- `backend/app/agents/path_agent.py`
- `backend/app/agents/orchestrator.py`
- `backend/app/agents/state.py`
- `backend/app/agents/graph.py`
- `backend/app/api/evaluate.py`
- `backend/app/api/path.py`
- `backend/app/api/resources.py`
- `backend/app/api/chat.py`
- `backend/app/api/tutor.py`
- `backend/app/api/meta.py`
- `backend/app/schemas/core.py`
- `backend/app/models/entities.py`
- `backend/app/models/db.py`
- `backend/app/config.py`
- `backend/app/llm/spark_client.py`

### 1.3 课程数据与前端

- `backend/app/data/knowledge_graph.json`
- `backend/app/data/seed_quiz.json`
- `frontend/lib/api.ts`
- `frontend/lib/types.ts`
- `frontend/app/page.tsx`
- `frontend/app/resources/page.tsx`
- `frontend/app/path/page.tsx`
- `frontend/app/eval/page.tsx`
- `frontend/components/chat/Chat.tsx`
- `frontend/components/resource/QuizPlayer.tsx`
- `frontend/components/profile/ProfileRadar.tsx`

### 1.4 只读数据

- SQLite：`backend/sparklearn.db`
- 只读 API：
  - `GET /api/profile/demo_user`
  - `GET /api/eval/report?user_id=demo_user`
  - `GET /api/path?user_id=demo_user`
- 画像演变证据：`backend/runs/*/request.json`、`profile.output.json`、`events.jsonl`、`final_state.json`

## 2. 画像数据结构与 demo_user 当前值

画像持久化在 `profiles` 表：

- 主键：`user_id`
- 画像主体：`data` JSON
- 版本：`version` 整数
- 更新时间：`updated_at`

`demo_user` 当前为：

- 用户名：`同学`
- 建档时间：`2026-06-12 20:06:28`
- 画像版本：`156`
- 画像更新时间：`2026-06-24 17:45:38`

### 2.1 八维画像

| 维度 | 设计含义 | 代码期望类型 | demo_user 当前真实值 | 可信度 |
| --- | --- | --- | --- | --- |
| `knowledge_mastery` | 各知识点 BKT 掌握概率 | `dict[str, float]`，值应为 0~1 | 见下方完整列表 | 部分可信，受无效 ID 和 smoke 答题污染 |
| `cognitive_style` | 视觉型/语言型/动手型 | `str` | `"语言型"` | LLM 推断，无稳定行为证据 |
| `error_prone` | 高频易错知识点/错误类型 | `list[str]` | `["概","念","混","淆","公式应用"]` | 已污染 |
| `goal` | 应试/竞赛/工程实践/兴趣 | `str` | `"竞赛"` | 会被每次画像 LLM 重写 |
| `pace` | 每日分钟数、频率、专注度 | `dict` | `{"daily_minutes":4,"frequency":"高","focus":"中"}` | 无时长证据，不可信 |
| `difficulty_pref` | 循序渐进/挑战式 | `str` | `"挑战式"` | 可用但下游存在枚举不一致 |
| `resource_pref` | doc/video/quiz/mindmap/code 偏好 | `dict[str, float]`，提示词规定 0~1 | `{"doc":0,"video":1,"quiz":1,"mindmap":31,"code":0}` | 非法量纲，不可信 |
| `metacognition` | 自评准确度、求助合理性 | `float`，0~1 | `0.229` | LLM 推断，缺乏可核验计算过程 |

其他说明字段：

```json
{
  "evidence": "高级排序"
}
```

该 evidence 不能解释八维值如何得出，不能作为企业审计意义上的数据血缘。

### 2.2 knowledge_mastery 当前完整值

```json
{
  "complexity": 0.3,
  "array": 0.3,
  "linked_list": 0.3,
  "stack": 0.3,
  "queue": 0.3,
  "recursion": 0.3,
  "sorting_basic": 0.2286,
  "sorting_adv": 0.2286,
  "binary_tree": 0.9997,
  "bst": 0.3,
  "heap": 0.3,
  "hash": 0.3,
  "graph_basic": 0.3,
  "graph_traverse": 0.3,
  "shortest_path": 0.3,
  "dp": 0.3,
  "complexity(复杂度分析)": 0.4407
}
```

## 3. 五条疑似脏数据核实与根因

### 3.1 重复知识点 ID：确认存在

数据库同时存在：

```text
complexity                 = 0.3000
complexity(复杂度分析)     = 0.4407
```

标准知识图谱只存在 `complexity`，不存在 `complexity(复杂度分析)`。

污染链路已完整还原：

1. `planner_agent.py` 告诉 LLM “kp 用 id”；
2. 但实际提供的候选列表格式是 `complexity(复杂度分析)`；
3. LLM 在运行 `s-dd1bj7` 中直接返回了这个完整展示串；
4. Planner 只解析 JSON，没有用 `all_kp_ids()` 校验返回值；
5. 返回值被写入 State 的 `knowledge_points`；
6. Doc/Mindmap/Quiz/Media 全部使用该字符串作为 `kp`；
7. 学生提交该题组后，`update_mastery()` 对任何字符串都允许写入，于是画像增加了新键。

直接证据：

```text
运行：backend/runs/s-dd1bj7
用户输入：帮我规划学习路劲
Planner 输出 kp：complexity(复杂度分析)
污染资源：4 条（doc/video/mindmap/quiz）
污染题目：5 道
污染答题记录：QuizAttempt id=7
```

根因不是画像和知识图谱各自维护了两套合法 ID，而是 Planner 输出缺少白名单校验，随后 BKT 写入也缺少知识图谱 ID 校验。

### 3.2 错因标签单字化：确认存在

当前值：

```json
["概", "念", "混", "淆", "公式应用"]
```

确定性代码根因：

1. `quiz_agent._validate()` 只保证 `error_tags` 字段存在，没有强制它必须是 `list[str]`；
2. 画像 LLM 曾在 `s-3kx8xx` 后把 `error_prone` 写成字符串 `"概念混淆"`；
3. `merge_profile()` 对非 list 的 `error_prone` 仍会走普通赋值，将错误类型存进数据库；
4. 下一次合并 list 时执行：

```python
[*data.get(k, []), *v]
```

5. 当旧值是字符串 `"概念混淆"` 时，`*旧值` 会按 Python 字符串迭代规则拆成：

```text
概、念、混、淆
```

运行证据：

```text
s-3kx8xx 画像 v140：error_prone = "概念混淆"
s-888xqz 画像 v141：error_prone = ["概","念","混","淆","概念混淆"]
当前 v156：["概","念","混","淆","公式应用"]
```

因此这是可重复的类型校验缺失，不是数据库乱码。

### 3.3 resource_pref：旧“全 0”已变化，但数据仍然脏

当前值：

```json
{
  "doc": 0,
  "video": 1,
  "quiz": 1,
  "mindmap": 31,
  "code": 0
}
```

提示词明确要求每项在 `0~1`，因此 `mindmap=31` 是非法值。

当前系统并没有按“点击率/完成率”计算资源偏好：

- `EventLog` 注释虽然列出 `open_resource/dwell/replay`，但 demo_user 当前只有：
  - `generate_done` 57 条
  - `quiz_eval` 13 条
  - `path_replan` 9 条
- 没有资源打开、停留、完成、重看事件；
- `profile_agent` 把最近 12 条事件原样拼成文本交给 LLM；
- `merge_profile()` 不校验范围、不归一化，直接深合并。

高置信根因证据：

- 多条 `quiz_eval` 事件包含 `mindmap_refresh.updated = 31`；
- 随后的 `58b495c8` 画像 v155 首次出现 `resource_pref.mindmap = 31`；
- 说明模型把“更新了 31 张旧脑图”的计数误识别成了“脑图偏好值”；
- 后端没有 `[0,1]` 边界校验，因此直接落库。

结论：旧状态的“全 0”只是模型曾输出全 0；当前也不是从真实偏好计算得出。

### 3.4 每日学习时间 4 分钟：确认存在

当前值：

```json
{"daily_minutes": 4, "frequency": "高", "focus": "中"}
```

这不是“时间统计公式算错”，因为当前代码根本没有学习时长计算：

- 79 条 demo_user 学习事件中没有 `duration`、`minutes`、`dwell`、`seconds` 等有效时长字段；
- `QuizPlayer` 虽提交 `seconds`，当前页面固定传 `0`；
- `evaluate_answers()` 不使用 seconds 更新 pace；
- `profile_agent` 只是让 LLM 根据对话和最近事件推断 `daily_minutes`。

第一次从 45 变为 4 的证据：

```text
画像 v30：daily_minutes = 45（Mock 默认）
画像 v31 / 运行 accb2117：daily_minutes = 4
```

v31 前提供给模型的近期行为主要是多条：

```json
{"etype":"generate_done","payload":{"n":4,"kinds":["doc","mindmap","quiz","video"]}}
```

因此“4”很可能来自每次生成 4 类资源的计数，而不是分钟数。即使不采纳这个具体来源推断，下面两点是确定事实：

1. 数据源里没有学习分钟数；
2. LLM 输出的 4 没有被证据门控和数值校验，直接落库。

### 3.5 前端固定 demo_user：确认存在

统一写死位置：

```ts
// frontend/lib/api.ts
export const USER_ID = "demo_user";
```

使用范围：

- 首页画像：`frontend/app/page.tsx`
- 学习对话：`frontend/components/chat/Chat.tsx`
- 资源生成、资源库、资料上传：`frontend/app/resources/page.tsx`
- 答题提交：`frontend/components/resource/QuizPlayer.tsx`
- 路径读取与重排：`frontend/app/path/page.tsx`
- 学情报告：`frontend/app/eval/page.tsx`

聊天页面还直接显示“当前用户：demo_user”。当前没有登录态、用户切换或会话隔离。

## 4. 知识点 ID 一致性审计

### 4.1 标准集合

`knowledge_graph.json` 共 16 个规范 ID：

```text
complexity, array, linked_list, stack, queue, recursion,
sorting_basic, sorting_adv, binary_tree, bst, heap, hash,
graph_basic, graph_traverse, shortest_path, dp
```

### 4.2 seed_quiz

种子题库中的 ID 全部是规范 ID，没有非法值。

覆盖缺口：

```text
graph_basic
graph_traverse
```

这两项是题库覆盖不足，不属于 ID 污染。

### 4.3 demo_user 不一致项

| 数据位置 | 非法 ID | 数量/值 |
| --- | --- | --- |
| `profiles.data.knowledge_mastery` | `complexity(复杂度分析)` | 0.4407 |
| `resources.kp` | `complexity(复杂度分析)` | 4 条 |
| quiz payload 中题目 `kp` | `complexity(复杂度分析)` | 5 道 |
| `quiz_attempts.kp` | `complexity(复杂度分析)` | 1 条，id=7 |

四条资源 ID：

```text
325c46f25979  doc
006c1e482922  video
0db41a2ff3fa  mindmap
aeb2247b27fd  quiz
```

### 4.4 全数据库其他不一致项

另有测试用户：

```text
phase2_real_user_fix → degree_of_a_node = 0.9319
```

其题组资源 `86463d826a1f` 内有 4 道题使用该非法 ID。

这进一步证明：当前所有进入 `update_mastery(user_id, kp, correct)` 的 kp 都缺少统一知识图谱白名单验证。

## 5. demo_user 当前累计数据

### 5.1 资源

总计 132 条：

| 类型 | 数量 |
| --- | ---: |
| doc | 27 |
| mindmap | 41 |
| quiz | 25 |
| video | 39 |

主要知识点：

```text
binary_tree                 94
sorting_basic                9
complexity                   7
recursion                    7
graph_basic                  5
complexity(复杂度分析)       4
sorting_adv                  4
dp                           2
```

### 5.2 学习事件

总计 79 条：

```text
generate_done  57
quiz_eval      13
path_replan     9
```

没有真正的点击、停留、重看、完成度事件，因此目前无法从行为日志计算 pace 和 resource_pref。

### 5.3 答题

- 答题轮次：13
- 数据库平均分：0.3256
- 时间跨度：2026-06-15 至 2026-06-22

其中多轮明确带有 smoke 标记，并包含同一题组连续“故意全错”和“故意全对”的自动验证。例如：

```text
backend_regrade_wrong_after_reload
backend_regrade_correct_after_reload
bff_wrong
bff_correct
```

所以 `binary_tree=0.9997` 只能证明 BKT 能被答题驱动，不能证明 demo_user 真正掌握了二叉树。

### 5.4 当前路径

```json
{
  "next_kp": "heap",
  "generated_by": "path_agent",
  "done": 1,
  "ready": 3,
  "locked": 12
}
```

该路径把 `binary_tree=0.9997` 当作已掌握，因此解锁了 BST、堆等节点。由于二叉树掌握度受 smoke 数据影响，这个推荐结果在工程上能解释，但在学生意义上不可信。

## 6. 个性化闭环可信度

### 6.1 当前代码数据流

```text
QuizPlayer 提交
→ POST /api/eval/submit
→ 从 Resource.payload.questions 还原标准答案
→ 后端 _grade_answer() 重判
→ evaluate_answers()
→ update_mastery()
→ profiles.data.knowledge_mastery
→ get_profile()
→ plan_path()
→ path_plans.data
→ /path 页面读取
→ 旧 mindmap 尝试重标注
```

“BKT 写入”和“路径读取”使用的是同一个字段：

```text
profiles.data.knowledge_mastery
```

不存在“写一张表、路径读另一张表”的断线。

### 6.2 可信环节

1. 当前后端客观题判分以数据库题组标准答案为准，不再信任前端传来的 `correct`。
2. BKT 公式是确定性的，同样输入会得到同样结果。
3. `update_mastery()` 会提交到 `profiles.data.knowledge_mastery`，刷新后仍保留。
4. `plan_path()` 读取同一画像字段，并把结果保存到 `path_plans`。
5. 对规范 ID 而言，答对上升、答错下降、路径随之变化的方向是可信的。

### 6.3 被脏数据影响的环节

1. `demo_user` 混入大量自动测试答题，当前掌握度不代表真实学生。
2. Planner 可生成非法展示型 ID，导致 BKT 写入知识图谱外的新键。
3. `error_prone` 单字化后，路径服务的错因匹配难以命中知识点，针对性排序失效。
4. `resource_pref=31` 会作为画像输入传给 Planner，可能严重扭曲资源类型选择。
5. `pace=4` 没有真实行为依据；当前 Planner 的 slim profile 未传 pace，所以它主要污染展示，暂未直接影响路径。
6. `difficulty_pref` 存在枚举不一致：
   - Profile 输出和默认约定：`挑战式`
   - Quiz 难度代码判断：`挑战型`
   - 因此当前 `挑战式` 不会触发 Quiz 的难度 +1。
7. Profile API 返回 `_version`，前端类型和组件读取 `version`，所以画像卡片可能一直显示 `v0`。
8. 前端把 `pace` 声明为 `string`，后端实际返回 `dict`，属于契约不一致。
9. `evaluate_answers()` 返回 `profile.get("version")`，实际版本字段是 `_version`，返回的 `profile_version` 实际为空。

### 6.4 总体判断

| 能力 | 状态 |
| --- | --- |
| 答题提交与当前客观题重判 | 已接通 |
| BKT 算法更新 | 已接通 |
| 掌握度持久化 | 已接通 |
| 路径读取同一掌握度 | 已接通 |
| 路径刷新后保持 | 已接通 |
| 知识点 ID 治理 | 未完成 |
| 画像字段类型/范围校验 | 未完成 |
| 行为数据采集 | 基本未建设 |
| pace/resource_pref 可解释计算 | 未完成 |
| demo_user 作为真实演示学生 | 当前不可信 |
| 多用户隔离 | 未完成 |

## 7. 建议由你拍板的清洗范围

### 建议一：demo_user 采用“重置行为画像”，不做局部修补

推荐原因：它已混入 smoke、历史逻辑和人工操作。仅删除一个非法 ID 无法恢复真实学习轨迹。

建议保留：

- 已生成的干净学习资源，可作为资源库演示；
- 原始运行留痕，供审计。

建议重置：

- `profiles` 中 demo_user 的八维画像；
- `quiz_attempts`；
- `event_logs`；
- `path_plans`；
- 受非法 ID 影响的 4 条资源单独隔离或删除。

备选方案是“只修脏字段”，但修完后的 `binary_tree=0.9997` 仍来自测试，不建议作为答辩画像。

### 建议二：画像写入前增加严格契约

建议限制：

- `knowledge_mastery`：只允许知识图谱 ID，值 clamp 到 0~1；
- `error_prone`：统一转成 `list[str]`，过滤单字和空值；
- `pace`：没有真实时长证据时不更新 `daily_minutes`；
- `resource_pref`：不再让 LLM凭事件文本自由填写，改由行为计数归一化；
- `difficulty_pref`：统一枚举为 `循序渐进|挑战式`；
- 拒绝未知字段，避免画像 JSON 无限长出新结构。

### 建议三：Planner 与评估入口都校验 kp

- Planner 输出后必须对照 `all_kp_ids()`；
- 兼容清洗 `complexity(复杂度分析) → complexity`，但新数据不再允许展示串进入；
- `update_mastery()` 再做一次最终防线校验；
- API 显式传入的 `knowledge_points` 也应验证。

### 建议四：先定义真实行为事件，再计算偏好

至少需要可复用现有事件机制记录：

- 打开资源；
- 完成资源；
- 停留秒数；
- 重看；
- 主动求助；
- 答题耗时。

`resource_pref` 应由这些行为确定性计算并归一化，`pace` 应按日期和真实时长聚合。LLM 可以解释结果，但不应创造基础数值。

### 建议五：测试用户与演示用户隔离

- 自动测试不再使用 `demo_user`；
- 浏览器演示使用一个干净学生；
- 企业化方向应由登录态决定 user_id，不能使用前端常量。

## 8. 下一轮建议实施顺序

### 第 1 步：写入防线

涉及：

- `services/profile_service.py`
- `agents/profile_agent.py`
- `agents/planner_agent.py`
- `agents/quiz_agent.py`
- 必要时 `schemas/core.py`

目标：先阻止新脏数据继续进入。

### 第 2 步：清洗已有数据

涉及：

- `profiles`
- `quiz_attempts`
- `event_logs`
- `path_plans`
- 4 条非法 ID 资源

执行前先导出只读备份和删除/重置清单。

### 第 3 步：建立可信行为统计

涉及：

- 复用现有接口和 EventLog；
- 补齐现有交互中的行为记录；
- 确定性计算 pace/resource_pref。

若必须新增接口或事件类型，应另开工单，由你拍板；不能在清洗工单里顺手扩张。

### 第 4 步：前端用户与契约修正

涉及：

- `frontend/lib/api.ts`
- 页面用户来源
- `frontend/lib/types.ts`
- `ProfileRadar.tsx`

目标：修复 demo_user 写死、`_version/version` 和 `pace` 类型不一致。

### 第 5 步：闭环验收

建议用一个全新学生完成：

1. 冷启动画像；
2. 同一知识点一组全对；
3. 同一知识点一组全错；
4. 验证掌握度方向相反；
5. 刷新后仍在；
6. 路径状态和顺序变化；
7. 错因保持完整词组；
8. resource_pref 始终在 0~1；
9. 未产生任何知识图谱外 ID。

## 9. 本轮未做事项

- 未修改任何 Python/TypeScript/JSON 源文件；
- 未修改 `.env`；
- 未执行 POST 请求；
- 未更新任何画像；
- 未删除答题、事件、资源；
- 未重建路径或索引；
- 未运行会写入测试数据的测试套件。

