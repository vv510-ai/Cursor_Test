# 现状摸底报告

生成时间: 2026-06-18

## 0. 本次只读范围

已按 Phase 0 要求读取:

- `AGENTS.md`
- `agents/sparklearn-live-integration/SKILL .md`
- `C:\Users\26054\Desktop\Codex推进手册.md`
- `backend/app/config.py`
- `backend/app/llm/__init__.py`
- `backend/app/llm/spark_client.py`
- `backend/app/llm/signing.py`
- `backend/app/llm/spark_ocr.py`
- `backend/app/llm/spark_tts.py`
- `backend/app/llm/spark_image.py`
- `backend/app/llm/seedance_client.py`
- `backend/app/safety/grounding.py`
- `backend/app/safety/guard.py`

额外只读确认:

- `.env` 当前不存在。
- `.gitignore` 已忽略 `.env`、`.venv/`、`backend/runs/`、`backend/app/data/vector_store.json`、上传资料等运行产物。
- 当前 `is_demo=True`。
- `AGENTS.md` 与 `agents/` 当前在 Git 状态中显示为未跟踪文件。

注意: 用户说明中的 `.agents/skills/sparklearn-live-integration` 在当前仓库根目录下不存在。实际读到的路径是 `agents/sparklearn-live-integration/SKILL .md`，且文件名中 `SKILL` 后有一个空格。

## 1. 常驻铁律摘要

从 `AGENTS.md` 与推进手册确认，本项目后续阶段必须遵守:

- LLM 文本大模型访问只允许走 `backend/app/llm/spark_client.py` 中的 `llm_complete()` / `llm_stream()`，不得在 agent 或脚本里自建新的文本大模型客户端。
- 不自创接口、智能体、SSE 事件类型。若确实需要新增，必须先报告确认。
- 事件一律经 `emitter.emit()` 发出；新增事件类型必须同步前端类型与 AgentTrace 归约逻辑。
- 并行节点写入 State 的字段必须在 `backend/app/agents/state.py` 带 reducer。
- JSON 列直接存取 Python dict/list，严禁手工 `json.dumps` / `loads` 入库。
- 分阶段推进，每个 Phase 完成后停下汇报，等待确认。

## 2. 当前配置与运行模式

`backend/app/config.py` 使用 `pydantic-settings` 读取 `.env`，缺失依赖时有纯环境变量兜底类。

关键环境变量:

- 文本大模型: `SPARK_API_PASSWORD`、`SPARK_BASE_URL`、`SPARK_X2_BASE_URL`
- 讯飞传统三元组: `SPARK_APPID`、`SPARK_API_KEY`、`SPARK_API_SECRET`
- Seedance: `ARK_API_KEY`、`ARK_BASE_URL`、`SEEDANCE_MODEL`
- 存储: `DATABASE_URL`、`MILVUS_URI`、`REDIS_URL`
- 检索增强相关: `EMBEDDING_BACKEND`、`RERANK_API_URL`、`RERANK_API_KEY`、`RERANK_TOP_K`
- 行为开关: `DEMO_MODE`、`TRACE_ENABLED`、`BACKEND_PORT`、`FRONTEND_ORIGIN`

当前本机状态:

- `.env` 不存在。
- `SPARK_API_PASSWORD` 为空。
- `DEMO_MODE=auto`。
- `is_demo()` 因 `SPARK_API_PASSWORD` 为空返回 `True`。
- 真实文本大模型不会被调用，`llm_complete()` / `llm_stream()` 会走 `MockEngine`。

`is_demo()` 三态逻辑:

- `DEMO_MODE` 取 `1` / `true` / `yes` / `on` 时，强制 demo，`is_demo()` 直接返回 `True`，即使填了 `SPARK_API_PASSWORD` 也仍会走 `MockEngine`。
- `DEMO_MODE` 取 `0` / `false` / `no` / `off` 时，强制真实，`is_demo()` 直接返回 `False`，即使 `SPARK_API_PASSWORD` 为空也会尝试真实调用，后续可能在真实 LLM 请求处因缺密钥失败。
- `DEMO_MODE=auto` 或其他非上述值时，按 `SPARK_API_PASSWORD` 是否为空判断:
  - `SPARK_API_PASSWORD` 为空 -> `is_demo()` 返回 `True`。
  - `SPARK_API_PASSWORD` 非空 -> `is_demo()` 返回 `False`，会自动切到真实文本大模型模式。

Phase 1 填密钥后的关键结论: 只要保持 `DEMO_MODE=auto`，填入 `SPARK_API_PASSWORD` 后文本 LLM 会自动从 MockEngine 切到真实模式；若用户想显式控制，可用 `DEMO_MODE=true/false` 覆盖。

## 3. 文本 LLM 管道现状

文件: `backend/app/llm/spark_client.py`

现状:

- 讯飞文本大模型走 OpenAI 兼容接口。
- 鉴权方式是单一 `SPARK_API_PASSWORD`，作为 `Authorization: Bearer ...` 的 API key 使用。
- 普通模型默认 base URL 为 `SPARK_BASE_URL=https://spark-api-open.xf-yun.com/v1/`。
- X2/reasoner 默认 base URL 为 `SPARK_X2_BASE_URL=https://spark-api-open.xf-yun.com/x2/`。
- 模型角色映射:
  - `ultra` -> `4.0Ultra`
  - `max` -> `generalv3.5`
  - `pro` -> `generalv3`
  - `lite` -> `lite`
  - `reasoner` -> `spark-x`

函数现状:

- `_client(x2)` 懒加载 `openai.OpenAI`，使用 `spark_api_password` 与对应 base URL。
- `llm_complete()` 是非流式统一入口。demo 模式直接返回 `MockEngine.complete()`；真实模式下按角色及 fallback 顺序调用星火。
- `llm_stream()` 是流式统一入口。demo 模式走 `MockEngine.stream()`；真实模式下直接按指定角色流式调用。
- `parse_json()` 负责 LLM JSON 输出容错解析。
- `get_spark_llm()` 返回 LangChain `ChatOpenAI` 实例，供扩展使用。后续若使用它，仍应谨慎确认是否符合本次计划“只通过 `llm_complete()` / `llm_stream()`”的限制。

初步风险:

- 非流式 `llm_complete()` 有 fallback；流式 `llm_stream()` 当前没有同等 fallback，真实流式调用若指定档位失败，可能直接抛错。
- 后续 smoke 脚本应直接调用 `llm_complete()` / `llm_stream()`，不要绕过到 `_client()` 或新建 OpenAI 客户端。

## 4. 多模态网关现状

### 4.1 讯飞 HMAC 签名

文件: `backend/app/llm/signing.py`

- `assemble_auth_url()` 使用 APPID 体系里的 `api_key` / `api_secret` 生成 HMAC-SHA256 URL 签名。
- TTS、OCR、文生图复用该签名逻辑。
- OpenAI 兼容文本大模型不使用这里的 HMAC，而是使用 `SPARK_API_PASSWORD`。

### 4.2 OCR / 图片理解

文件: `backend/app/llm/spark_ocr.py`

- 入口: `image_to_question(image_bytes, hint="")`
- demo 模式直接返回固定演示题目文本。
- 非 demo 且缺少 `SPARK_APPID` / `SPARK_API_KEY` / `SPARK_API_SECRET` 时，返回“未配置三元组”的提示文本，不抛错。
- 真实调用使用 `wss://spark-api.cn-huabei-1.xf-yun.com/v2.1/image`。
- 依赖 `websocket-client`；缺依赖时返回提示文本。

初步风险:

- 缺密钥或缺依赖时返回的是普通字符串，调用方需要能区分“真实 OCR 文本”和“降级提示文本”。

### 4.3 TTS

文件: `backend/app/llm/spark_tts.py`

- 入口: `synthesize(text, voice="xiaoyan", filename=None)`
- 需要 `SPARK_APPID` / `SPARK_API_KEY` / `SPARK_API_SECRET`。
- 缺三元组时返回 `None`，由上层保留台词文本。
- 真实调用使用 `wss://tts-api.xfyun.cn/v2/tts`。
- 成功后写入 `backend/app/static/gen/*.mp3`，返回 `/static/gen/<filename>.mp3`。

### 4.4 文生图

文件: `backend/app/llm/spark_image.py`

- 入口: `generate_image(prompt, width=1024, height=768)`
- 需要 `SPARK_APPID` / `SPARK_API_KEY` / `SPARK_API_SECRET`。
- 缺三元组时返回 `None`。
- 真实调用使用 `https://spark-api.cn-huabei-1.xf-yun.com/v2.1/tti`。
- 成功后写入 `backend/app/static/gen/*.jpg`，返回 `/static/gen/<filename>.jpg`。

初步风险:

- 异常只记录 warning 并返回 `None`，后续真实联调需要通过产物是否存在、日志和返回路径共同判断，不能只看主链路没报错。

### 4.5 Seedance 视频

文件: `backend/app/llm/seedance_client.py`

- `available()` 要求同时配置 `ARK_API_KEY` 与 `SEEDANCE_MODEL`。
- `create_task()` 向 `ARK_BASE_URL/contents/generations/tasks` 提交任务。
- `poll_task()` 查询任务状态；未配置时返回 `status=degraded`。
- `gen_video_async()` 供 Media Agent 使用；未配置时返回 `status=degraded`，配置后创建任务并轮询，失败/异常也返回降级或失败状态。

初步风险:

- `create_task()` 本身不检查 `available()`，如果外部直接调用且缺配置，会携带空 key/model 请求。Media Agent 走 `gen_video_async()` 时是安全的。
- `poll_task()` 对成功结果的 URL 提取路径为 `(data.get("content") or {}).get("video_url")`，真实返回字段需要 Phase 3/Seedance 冒烟时确认。

## 5. 内容安全现状

文件: `backend/app/safety/guard.py`

同层补读文件: `backend/app/safety/grounding.py`

现状:

- 本地敏感词表 `_BLOCKLIST = ["代写论文", "考试作弊", "答案泄露", "枪手"]`。
- `run_local_blocklist()` 返回 `(ok, hits)`。
- `call_iflytek_audit()` 目前是挂接点:
  - demo 模式直接放行。
  - 非 demo 模式仍直接 `return True`。
  - TODO 注释写明后续接 `https://audit.iflyaisol.com`，HMAC 鉴权同 `signing.py`。
- `safety_filter()` 返回 `{"passed": passed, "hits": hits, "text": cleaned}`。
- `strip_pii()` 对手机号和身份证做简单遮蔽。

`grounding.py` 现状:

- `grounding_check(answer, contexts)` 是 X2 答案-证据一致性裁决入口。
- 它从 `..llm.spark_client` 导入的是 `llm_complete` 与 `parse_json`。
- 实际调用为 `await llm_complete(..., role="reasoner", temperature=0, json_mode=True)`。
- `role="reasoner"` 在 `spark_client.py` 中映射到 `spark-x`，走 `SPARK_X2_BASE_URL`。
- 未发现 `grounding.py` 自建 OpenAI/HTTP 客户端、直接调 `_client()`、直接调 requests/httpx/websocket；它符合 AGENTS.md 的 LLM 统一入口铁律。
- 若 X2 裁决异常，当前会 warning 并返回 `{"grounded": False, "unsupported": [], "verdict": "校验服务异常:..."}`，调用方会把它视为 grounding 风险。

已发现的接口不一致:

- `safety_filter()` 返回字段是 `passed`。
- 全仓只读检索结果显示，`safety_filter()` 直接调用方只有两个:

| 调用方 | 是否调用 `safety_filter()` | 读取字段 | 是否匹配返回值 | 后果 |
| --- | --- | --- | --- | --- |
| `backend/app/agents/doc_agent.py` | 是，过滤生成的 Markdown 文档 | `safety.get("safe", True)` | 不匹配，应读 `passed` | 本地敏感词或审核不通过时，`passed=False` 不会被识别，doc 的 safety 风险可能不会进入 `safety_flags`；敏感词虽被 `text` 字段替换，但调用方当前没有使用清洗后的 `safety["text"]` 覆盖原文。 |
| `backend/app/agents/tutor_agent.py` | 是，过滤 tutor 答案 | `safety.get("safe", True)` | 不匹配，应读 `passed` | tutor 答案命中敏感词时，风险可能不会进入 `safety_flags`；同样未使用清洗后的 `safety["text"]` 覆盖输出。 |
| `backend/app/agents/quiz_agent.py` | 否 | 无 | 无 | 不直接受 `passed/safe` 字段影响。 |
| `backend/app/agents/media_agent.py` | 否 | 无 | 无 | 不直接受 `passed/safe` 字段影响。 |
| `backend/app/agents/eval_agent.py` | 否；只汇总 `state["safety_flags"]` | 无 | 无 | 上游 doc/tutor 因字段读错漏记时，eval 只能汇总到不完整的 `safety_flags`。 |
| `backend/app/agents/orchestrator.py` | 否 | 无 | 无 | 不直接受 `passed/safe` 字段影响。 |
| 其他后端/前端/文档 | 未发现直接调用 | 无 | 无 | 暂无直接影响。 |

结论: 这是 Phase 4 内容审核或独立 bugfix 候选。Phase 0 不修。

## 6. 当前真实联调前置结论

当前系统仍处于离线 demo 状态，适合继续跑现有离线测试和演示；要切到真实服务，至少需要:

- 文本大模型: 填 `SPARK_API_PASSWORD`。
- X2/reasoner: 仍使用 `SPARK_API_PASSWORD`，但走 `SPARK_X2_BASE_URL` 与 `spark-x`。
- OCR/TTS/文生图: 填 `SPARK_APPID`、`SPARK_API_KEY`、`SPARK_API_SECRET`。
- Seedance: 填 `ARK_API_KEY` 与 `SEEDANCE_MODEL`。
- 若要容器化真实联调，还需确认 `.env` 被 docker compose 正确注入。

目前不应直接进入真实调用，除非用户提供或确认 `.env` 中的真实密钥。

## 7. Phase 0 待确认事项

1. 执行计划正文在本次消息中是占位文本，没有粘贴完整 6-Phase 原文。当前 Phase 0 是按用户消息和 `Codex推进手册.md` 中的 Phase 0 描述执行的。
2. 仓库实际 skill 目录是 `agents/`，不是说明中的 `.agents/skills/`。是否需要后续改回计划中的路径，需用户确认；Phase 0 不改。
3. `AGENTS.md` 与 `agents/` 当前未跟踪，若希望这些规则稳定纳入仓库，后续需确认是否提交。
4. 内容审核真实 API 未接，且 `safety_filter()` 字段与调用方存在 `passed`/`safe` 不一致，需要后续阶段处理或记录为已知问题。
5. 真实模式下 `llm_stream()` 没有 `llm_complete()` 那样的多档 fallback；指定档位失败会直接抛错。主链路中的 tutor 答疑与 SSE 推送依赖流式输出，Phase 2 应重点验证流式失败路径。若需要给 `llm_stream()` 补同等 fallback，这属于改主链路行为，必须先报告并等待用户确认，不能擅自动。
6. 本次 Phase 0 读到了仓库外文件 `C:\Users\26054\Desktop\Codex推进手册.md`。后续真实联调若要可复现，不应依赖仓库外文件；需用户确认是否把这份手册纳入仓库或 `.agents` 目录。
7. 当前 skill 文件名是 `SKILL .md`，`SKILL` 后多一个空格；这可能导致后续按规范路径 `SKILL.md` 找不到 skill。Phase 0 不改，但建议尽早由用户确认是否重命名。
